<?xml version="1.0"?>
<ruleset name="My PHPMD Rules">
    <description>Custom PHPMD ruleset for my PHP application</description>

    <!-- Specify the rule sets to be applied -->
    <rule ref="rulesets/cleancode.xml"/>
    <rule ref="rulesets/codesize.xml"/>
    <rule ref="rulesets/design.xml"/>
    <rule ref="rulesets/naming.xml"/>
    <rule ref="rulesets/unusedcode.xml"/>

    <!-- Add or customize rules as needed -->
    <!-- Example custom rule: -->
    <!-- <rule ref="category/my-custom-rule"/> -->
</ruleset>
